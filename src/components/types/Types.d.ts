export type Project = {
    id?: `${string}-${string}-${string}-${string}-${string}`
    projectName: string,
    clientName: string,
    service: string,
    totalPages: number,
    totalLanguages: number,
    totalPrice: number,

}